

const SearchChooseButton = (props) => {

    return(
        <div className="Search-Choose-Button-Wrapper">
            <button>{props.buttonName}</button>
        </div>
    )

}

export default SearchChooseButton;